resolver: lts-7.14
compiler: ghc-8.0

nix:
  enable: true
  pure: true
  packages: []
  # shell-file: shell.nix

nix:
  enable: true
  pure: true
  packages: []

packages:
- .
- ../spiros

extra-deps:
- spiros-0.0.0
